import styles from '../style';
import { robot,discount } from '../assets';

const Hero = () => {
  return (
    <section id='home' className={` ${styles.paddingY} `} >
      Hero
    </section>
  )
}

export default Hero
